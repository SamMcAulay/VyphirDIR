{
  "name": "vyphir-directory",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-characters.js && node scripts/generate-commissions-preview.js",
    "test": "node --test"
  },
  "devDependencies": {
    "gifenc": "^1.0.3",
    "sharp": "^0.35.3"
  }
}
